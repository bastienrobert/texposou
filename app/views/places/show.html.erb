<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @place.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @place.description %>
</p>

<p>
  <strong>Address:</strong>
  <%= @place.address %>
</p>

<p>
  <strong>Zipcode:</strong>
  <%= @place.zipcode %>
</p>

<p>
  <strong>City:</strong>
  <%= @place.city %>
</p>

<p>
  <strong>Area max:</strong>
  <%= @place.address %>
</p>
<strong>User:</strong>
<%= @place.user %>
</p>

<h2>Liste des exhibitions</h2>
  <% @place.exhibitions.each do |exhibition| %>
  <li><%= link_to exhibition.name, exhibition %></li>
  <% end %>
</ul>

<h2>Liste des images</h2>
  <% @place.image_places.each do |i| %>
   <%= image_tag i.file.url(:thumb) %> <%= i.id %>
  <% end %>
</ul>

<%= link_to 'Add exhibition', add_exhibition_path(@place) %> |
<%= link_to 'Edit', edit_place_path(@place) %> |
<%= link_to 'Back', places_path %>
