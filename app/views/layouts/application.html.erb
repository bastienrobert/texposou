<!DOCTYPE html>
<html>
  <head>
    <title>Texposou</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <!-- <nav class="navbar navbar-default">
        <div class="container-fluid">

          <div class="navbar-header">
            <%= link_to image_tag("Logotype.png"), root_path, id: "logo-brand", class:"navbar-brand", alt:'logo-texposou' %>
          </div>
          <% if user_signed_in? %>
          <p class="navbar-left navbar-text bvn-msg"> Bienvenue <%= link_to current_user.email, edit_user_registration_path %> !</p>
          <% end %>

          <div id="navbar-btns">
            <p class="navbar-right navbar-text btn-header">
              <span><%= link_to "s'inscrire", new_user_registration_path %></span>
            </p>
            <% if user_signed_in? %>
            <p class="navbar-right navbar-text btn-header"><%= link_to "se déconnecter", destroy_user_session_path, method: :delete, confirm: 'Are you sure ?' %></p>
            <% else %>
            <p class="navbar-right navbar-text btn-header"><%= link_to "se connecter", new_user_session_path %></p>
            <% end %>
            <p class="navbar-right navbar-text btn-header"><%= link_to "a propos", root_path %></p>

          </div>

        </div>
      </nav> -->

      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <!-- Brand and toggle get grouped for better mobile display -->
          <div class="navbar-header">
            <%= link_to image_tag("Logotype.png"), root_path, id: "logo-brand", class:"navbar-brand", alt:'logo-texposou' %>

            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false" id="menu-burger">
              <span class="sr-only">Toggle navigation</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
          </div>

          <!-- Collect the nav links, forms, and other content for toggling -->
          <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <% if user_signed_in? %>
            <p class="navbar-left bvn-msg"> Bienvenue <%= link_to current_user.email, edit_user_registration_path %> !</p>
            <% end %>

            <ul class="nav navbar-nav" id="btns-container">

              <li class="btn-header">
                <p class="navbar-right"><%= link_to "a propos", root_path %></p>
              </li>

              <li class="btn-header">
                <% if user_signed_in? %>
                <p class="navbar-right"><%= link_to "se déconnecter", destroy_user_session_path, method: :delete, confirm: 'Are you sure ?' %></p>
                <% else %>
                <p class="navbar-right"><%= link_to "se connecter", new_user_session_path %></p>
                <% end %>
              </li>

              <li class="btn-header">
                <p class="navbar-right">
                <span><%= link_to "s'inscrire", new_user_registration_path %></span>
                </p>
              </li>

            </ul>
          </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
      </nav>
    </header>
    <%= yield %>
  </body>
</html>
