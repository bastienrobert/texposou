<%= form_for(visit) do |f| %>
  <% if visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
      <% visit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :coming_at %>
    <%= f.datetime_select :coming_at %>
  </div>

  <div class="field">
    <%= f.label :duration %>
    <%= f.datetime_select :duration %>
  </div>

  <%= f.hidden_field :exhibition_id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
