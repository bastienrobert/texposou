<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @exhibition.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @exhibition.description %>
</p>

<p>
  <strong>Opening at:</strong>
  <%= @exhibition.opening_at %>
</p>

<p>
  <strong>Closing at:</strong>
  <%= @exhibition.closing_at %>
</p>

<p>
  <strong>Artists max:</strong>
  <%= @exhibition.artists_max %>
</p>

<p>
  <strong>Area left:</strong>
  <%= @exhibition.area_left %>
</p>

<p>
  <strong>Area max:</strong>
  <%= @exhibition.area_max %>
</p>

<p>
  <strong>Lieu :</strong>
  <%= @place.address %> <%= @place.zipcode %> <%= @place.city %>
</p>

<p>
  <strong>Price per day per area:</strong>
  <%= @exhibition.price_per_day_per_area %>
</p>

  <h2>Liste des visites</h2>
  <% @exhibition.visits.each do |visit| %>
    <li><%= link_to visit.user.id, visit %></li>
  <% end %>
  <%= link_to "Je participe", add_visit_path %>

  <h2>Liste des tags</h2>
  <% @exhibition.art_tags.each do |artTag| %>
    <li><%= artTag.name %> <%= artTag.id %></li>
  <% end %>

</ul>
<%= link_to 'Participer', add_participation_path(@exhibition) %>
<%= link_to 'Add visit', add_visit_path(@exhibition) %>
<%= link_to 'Edit', edit_exhibition_path(@exhibition) %> |
<%= link_to 'Back', exhibitions_path %>
